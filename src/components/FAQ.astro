---
type Item = { question: string; answer: string };
type Props = { items: Item[]; title?: string };

const { items, title = "Preguntas frecuentes" } = Astro.props;
---

<section aria-labelledby="faq-heading">
  <h2 id="faq-heading" class="text-2xl text-ink">{title}</h2>
  <div class="mt-6 divide-y divide-border rounded-[3px] border border-border">
    {
      items.map((item) => (
        <details class="group p-4 sm:p-5">
          <summary class="flex min-h-[44px] cursor-pointer list-none items-center justify-between gap-4 font-semibold text-ink marker:content-none">
            <span class="min-w-0">{item.question}</span>
            <span aria-hidden="true" class="shrink-0 text-brand transition-transform group-open:rotate-45">+</span>
          </summary>
          <p class="mt-3 text-sm text-ink-soft">{item.answer}</p>
        </details>
      ))
    }
  </div>
</section>
