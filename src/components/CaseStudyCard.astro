---
type Props = {
  title: string;
  service: string;
  location: string;
  clientType: string;
  result: string;
  href: string;
  draft?: boolean;
};

const { title, service, location, clientType, result, href, draft = false } = Astro.props;
---

<article class="flex flex-col gap-3 rounded-lg border border-border bg-paper p-6">
  {draft && (
    <p class="w-fit rounded-full bg-surface px-3 py-1 text-xs font-semibold uppercase tracking-wide text-ink-soft">
      Borrador · pendiente de confirmar
    </p>
  )}
  <p class="text-xs font-semibold uppercase tracking-wide text-brand">{service} · {location}</p>
  <h3 class="font-heading text-xl font-bold text-ink">
    <a href={href} class="hover:text-brand">{title}</a>
  </h3>
  <p class="text-sm text-ink-soft"><strong class="text-ink">Tipo de cliente:</strong> {clientType}</p>
  <p class="text-sm text-ink-soft"><strong class="text-ink">Resultado:</strong> {result}</p>
  <a href={href} class="mt-1 inline-flex items-center gap-1 font-semibold text-brand hover:underline">
    Ver caso
    <span aria-hidden="true">→</span>
  </a>
</article>
